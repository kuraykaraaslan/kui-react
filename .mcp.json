{
  "$schema": "https://modelcontextprotocol.io/schemas/v1/mcp.json",
  "mcpServers": {
    "kui-react-registry": {
      "command": "node",
      "args": ["scripts/mcp-server.mjs"],
      "description": "Exposes the kui-react component registry (components, themes, design tokens, conventions, source) as MCP tools. Reads from public/registry/components.json — rebuild via `npm run registry:snapshot`."
    }
  }
}
