'use client';
// modules/ui/Chart/charts/RadarChart.tsx
//
// M3 stub. Returns null. Implement in M3.
//
// TODO M3: polar radial axes (one per category), polygonal series fill,
// token-aware grid rings. Replaces the legacy ProductComparisonRadar
// in modules/domains/common/charts/Charts.tsx when M5 migration lands.

import type { BaseChartProps } from '../types';

export function RadarChart(_props: BaseChartProps) {
  // TODO M3: implement multi-axis radar / spider chart.
  return null;
}
