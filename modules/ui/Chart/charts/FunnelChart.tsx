'use client';
// modules/ui/Chart/charts/FunnelChart.tsx
//
// M3 stub. Returns null. Implement in M3.
//
// TODO M3: trapezoidal funnel stages, conversion-rate label between
// stages, token-aware fills (gradient from `--primary` to `--secondary`).

import type { BaseChartProps } from '../types';

export function FunnelChart(_props: BaseChartProps) {
  // TODO M3: implement funnel stages + conversion labels.
  return null;
}
