'use client';
/**
 * @deprecated Use `<DataTable mode="server" fetchPage={...} />`.
 * Backwards-compatible shim — `ServerDataTable` now lives in `modules/ui/Table/`.
 */
export { ServerDataTable } from './Table/index';
export type { TableColumn } from './Table/index';
