'use client';
// modules/ui/ColorPicker/parts/GradientEditor.tsx
//
// TODO M3: multi-stop linear/radial gradient editor that outputs a CSS
// `linear-gradient(...)` / `radial-gradient(...)` string.
// Stub only.

type GradientEditorProps = {
  mode?: 'linear' | 'radial';
  value?: string;
  onChange?: (css: string) => void;
};

export function GradientEditor(_props: GradientEditorProps) {
  // TODO M3
  return null;
}
