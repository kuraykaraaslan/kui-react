'use client';
// modules/ui/ColorPicker/parts/EyeDropper.tsx
//
// TODO M2: native EyeDropper button — uses useEyeDropper() hook.
// Stub only.

type EyeDropperProps = {
  onPick?: (color: string) => void;
};

export function EyeDropper(_props: EyeDropperProps) {
  // TODO M2
  return null;
}
