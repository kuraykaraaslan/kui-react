// modules/ui/Slider/hooks/useLoop.ts
//
// TODO M3: Virtual infinite loop — clone N slides on each side and
//          reposition the track on the edge without animation so the user
//          can drag past the boundary seamlessly.
// Stub kept so the directory shape matches PLANS/29.

export function useLoop() {
  // Intentional no-op until M3.
}
