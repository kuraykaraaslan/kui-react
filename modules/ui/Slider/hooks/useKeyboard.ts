// modules/ui/Slider/hooks/useKeyboard.ts
//
// TODO M5: WAI-ARIA keyboard navigation — ArrowLeft/Right, Home, End, Space (pause/play).
// Stub kept so the hook surface exists and downstream files can import safely.

export function useKeyboard() {
  // Intentional no-op until M5.
}
