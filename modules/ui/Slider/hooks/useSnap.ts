// modules/ui/Slider/hooks/useSnap.ts
//
// TODO M2: CSS scroll-snap setup + JS fallback for snap-to-slide when
//          slidesPerView > 1 / slidesToScroll mismatches.
// Stub kept so the directory shape matches PLANS/29.

export function useSnap() {
  // Intentional no-op until M2.
}
