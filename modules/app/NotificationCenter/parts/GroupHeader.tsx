'use client';
// ─── GroupHeader — M2 stub ───────────────────────────────────────────────────
// M2 will render section labels like "GitHub · 3 new" or "Today" between
// notification groups. Hidden for M1 — flat list only.

// TODO M2: render ({ label, count, collapsible }) and collapsible toggle.
export function GroupHeader(): React.ReactElement | null {
  return null;
}
