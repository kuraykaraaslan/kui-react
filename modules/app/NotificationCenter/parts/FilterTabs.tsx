'use client';
// ─── FilterTabs — M2 stub ────────────────────────────────────────────────────
// M2 will render All / Unread / Mentions / Assigned tabs above the list.
// For M1 we export a null component so the panel layout stays stable.

// TODO M2: implement tabs ({ id, label, count }), bind active tab to
//          FilterState in useNotifications.
export function FilterTabs(): React.ReactElement | null {
  return null;
}
