'use client';
// TODO M2: column reorder via drag on column header.
// API sketch:
//   const { onColumnDragStart, onColumnDragOver, onColumnDrop, draggingColumnId } =
//     useColumnReorder({ columns, onColumnReorder });
export function useColumnReorder() {
  // TODO M2: implement HTML5 DnD on column headers, persist new order via onColumnReorder.
  return {};
}
