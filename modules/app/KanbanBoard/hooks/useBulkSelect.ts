'use client';
// TODO M4: bulk card selection (Shift+click range, Cmd/Ctrl+click toggle).
// API sketch:
//   const { selectedIds, toggle, selectRange, clear, isSelected } =
//     useBulkSelect({ cards });
//   // -> drag any selected card to move the whole batch.
export function useBulkSelect() {
  // TODO M4: maintain selectedIds Set, anchor for range select, bulk move via onCardMove.
  return {};
}
