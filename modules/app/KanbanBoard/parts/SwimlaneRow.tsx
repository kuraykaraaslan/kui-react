'use client';
// TODO M3: Swimlane row — groups columns horizontally under a shared lane title
// (e.g. group by assignee / priority / label). For M1, KanbanBoard renders a
// single implicit lane and this component is unused.
//
// API sketch:
//   <SwimlaneRow lane={lane}>
//     {columns.map(col => <Column ... />)}
//   </SwimlaneRow>
export function SwimlaneRow() {
  return null;
}
