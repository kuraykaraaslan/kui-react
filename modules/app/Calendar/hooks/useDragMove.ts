'use client';
// TODO M2: drag an event between cells with snap-to-interval and conflict
// preview. Returns ref+handlers wired to <EventCard>.

export function useDragMove() {
  // TODO M2: implement pointer-driven move with onEventUpdate callback.
  return {
    enabled: false as const,
  };
}
