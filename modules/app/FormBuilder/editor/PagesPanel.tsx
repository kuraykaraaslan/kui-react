'use client';
// TODO M4 — Multi-page support.
//
// Renders a tab strip at the top of the canvas, one per page in the schema.
// Active page index lives in builder state. Each FieldRow drops into the
// active page's fields[] array. Progress bar inside FormRenderer mirrors the
// same tabs.
//
// For M1 we keep a single implicit page.

export function PagesPanel() {
  return null;
}
