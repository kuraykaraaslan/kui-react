'use client';
// TODO M5 — Star / numeric rating control.
import type { Field } from '../../types';

type Props = {
  field: Field;
  value: unknown;
  error?: string;
  onChange: (rating: number) => void;
};

export function RatingField(_props: Props) {
  // TODO M5 — implement.
  return null;
}
